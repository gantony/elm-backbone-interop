# Elm - Backbone interop

This repository demonstrates how we can interop between and Elm application and a Backbone model.

## Build step

`elm-make ElmView.elm --output=build/ElmView.js` or `./build.sh`

Then run using: `elm-reactor`

